@using AdminSite.Models
@model AdminSite.Models.GTSListModel
@{
    Layout = "/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "GTS";
}

@foreach (var thing in Model.GTS)
{
    @await Html.PartialAsync("PartialGTSClosed", thing);
}